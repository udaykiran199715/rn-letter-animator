/**
 * Default animation duration (milliseconds).
 */
export const DEFAULT_DURATION = 3000;

/**
 * Default animation delay (milliseconds).
 */
export const DEFAULT_DELAY = 0;

/**
 * Default animation progress.
 */
export const DEFAULT_PROGRESS = 0;

/**
 * Minimum timeline progress.
 */
export const MIN_PROGRESS = 0;

/**
 * Maximum timeline progress.
 */
export const MAX_PROGRESS = 1;
